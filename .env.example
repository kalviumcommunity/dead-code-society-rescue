# Server port — the port the Express server listens on
PORT=3000

# MongoDB connection string — full URI including database name
DATABASE_URL=mongodb://localhost:27017/logitrack

# JWT secret — used to sign and verify authentication tokens
# IMPORTANT: Use a strong, random string in production (min 32 chars)
JWT_SECRET=change_me_to_a_strong_random_secret
